BUG FIX: Ripple Effect node graph is rendering with wrong topology.

Confirmed bugs from visual inspection:
1. "SPR Buffer Drawdown" node has zero connected edges — the
   corridor → spr edge is missing entirely from the render.
2. "Strait of Hormuz" (corridor) origin node is not positioned at the
   top as the single source — it's floating mid-graph with edges
   looping into and out of it in a way that doesn't match a top-down
   cascade.
3. A corridor → gdp direct edge appears to be rendering, bypassing
   refinery. Per the agreed data structure, GDP drag and fuel price
   shock must ONLY be reachable through refinery — there is no direct
   corridor → gdp or corridor → price_shock edge. If this edge exists
   in the render, the edge list in code does not match the approved
   spec — find and fix that mismatch.
4. At least one arrow points in the wrong direction (arrowhead points
   INTO a node that should be the source, not the target). Verify
   every edge's arrowhead renders at the `to` node, never the `from`
   node.

REQUIRED FIX — enforce this exact layout:
- corridor: top center, single origin, no incoming edges
- refinery and spr: second row, both receive an edge FROM corridor
  only (corridor → refinery, corridor → spr) — these are the only
  two edges with hop=1
- price_shock and gdp: third row, both receive an edge FROM refinery
  only (refinery → price_shock, refinery → gdp) — these are the only
  two edges with hop=2
- spr has no outgoing edges (it's a terminal node)
- Use a simple top-down tree layout (corridor at y:15, refinery+spr
  at y:45, price_shock+gdp at y:75) with straight or gently-curved
  lines that do NOT cross each other — re-derive x positions so edges
  don't overlap visually (e.g. refinery left, spr right, then
  price_shock/gdp positioned directly below their actual parent
  (refinery), not spread evenly across the row).

Before re-rendering, print/log the actual edge array being passed to
the SVG renderer and confirm it contains exactly these 4 edges and no
others:
  corridor→refinery, corridor→spr, refinery→price_shock, refinery→gdp
If the logged array contains a 5th edge or a corridor→gdp/price_shock
edge, the bug is in the data layer, not the rendering layer — fix
the data first.