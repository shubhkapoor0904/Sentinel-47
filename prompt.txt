TASK: Add a "Sentinel Resilience Index" — a single composite national
score — to the persistent top strip. Do NOT touch any existing module's
calculation logic. This is purely a new derived metric that READS from
data your 4 modules already compute; it must not introduce any new
independent calculations.

WHAT TO BUILD
A single number, 0-100, displayed prominently in the top strip
alongside the existing Brent price / corridor risk indicators —
e.g. "INDIA RESILIENCE SCORE: 62" with a small colored ring/badge
(green 70+, amber 40-69, red <40).

COMPOSITE FORMULA (reuse existing live values, weight them — do not
invent new inputs):
  score = 100 - weighted_average(
    corridor_risk_component,      weight: 0.30   // from Module 1's
                                                   // live corridor %s
                                                   // (avg of Hormuz/
                                                   // Red Sea/Suez)
    spr_depletion_component,      weight: 0.30   // from Module 2's
                                                   // SPR buffer days,
                                                   // normalized against
                                                   // the 9.5-day baseline
    refinery_exposure_component,  weight: 0.25   // from Module 2's
                                                   // refinery run-rate
                                                   // drop %
    gdp_drag_component,           weight: 0.15   // from Module 2's
                                                   // GDP drag %
  )
  Normalize each component to a 0-100 sub-score before weighting
  (e.g. corridor_risk_component = 100 - avg_corridor_risk_pct).
  If no scenario is active in Module 2, use baseline/stable values
  for those components so the score still renders meaningfully at
  rest (should read high, e.g. 85-95, when nothing is active).

INTERACTIVITY
- The score MUST update live when corridor risk changes (Module 1
  refresh) or when a scenario is selected in Module 2 — pull from
  the same shared state those modules already write to, don't poll
  separately.
- Animate the score number with a tween (400-600ms) on change, same
  pattern as other live metrics in the app.
- Clicking the score badge opens a small breakdown popover showing
  the 4 weighted components and their current sub-scores, so it's
  never a black-box number — this is critical for judge credibility.

LABELING (matches existing data-honesty convention)
Add a small caption under/near the score: "COMPOSITE — DERIVED FROM
LIVE + MODELED MODULE DATA" and in the breakdown popover, label each
component as either LIVE (corridor risk, since it's news-derived) or
MODELED (SPR/refinery/GDP, since those come from scenario simulation).

SCALABILITY FRAMING (for the pitch, not the UI)
Add one line of code comment near the formula: "Methodology
generalizes to any import-dependent economy or critical commodity by
substituting corridor/reserve/GDP inputs — scoring structure is
commodity-agnostic." This is just a comment for your own reference
when explaining scalability to judges, not user-facing text.

CONSTRAINTS
- This must be a pure derived/computed display value — verify it has
  no independent data fetch of its own, only reads from existing
  module state.
- Confirm the formula's 4 weights sum to 1.0 exactly.

Show me the exact normalization logic for each of the 4 components
before wiring it into the top strip.