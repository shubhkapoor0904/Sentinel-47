TASK: Convert single-point modeled metrics in Module 2 (Disruption
Scenario Modeller) to ranges. Do NOT touch the underlying calculation
engine, scenario presets, or any other module — this is a display
formatting change only, applied to the 4 existing metric cards
(Refinery Run Rate, Fuel Price Shock, SPR Buffer Cover, Quarterly GDP
Drag).

WHAT TO CHANGE
For each metric, instead of displaying one exact computed value,
compute and display a range: [computed_value - spread, computed_value
+ spread], where spread is a fixed, clearly-defined percentage of the
computed value (e.g. ±15%) — pick ONE consistent spread percentage
and apply it uniformly across all 4 metrics, don't vary it per metric
without a stated reason.

Example transformations:
- "Refinery Run Rate: 79%" → "Refinery Run Rate: 73%–85%"
- "Fuel Price Shock: +₹14.00/litre" → "+₹11.90–₹16.10/litre"
- "SPR Buffer Cover: 5.8 days left" → "5.0–6.6 days left"
- "Quarterly GDP Drag: -0.80%" → "-0.68% to -0.92%"

DISPLAY TREATMENT
- Show the range as the primary number (not a small subtext) — this
  is a credibility upgrade, not a footnote.
- Keep the existing before/after bar visual, but change it to show
  a shaded BAND (min-to-max) instead of a single bar endpoint, with
  the original point-estimate shown as a thin marker line inside
  the band, so users can still see "most likely value" within the
  range.
- Add a small inline label near each metric: "MODELED RANGE" using
  the same caption styling as existing "MODELED ILLUSTRATIVE DATA"
  labels elsewhere in the app — keep the data-honesty convention
  consistent.

WHY (for context, not for UI)
Single-point estimates overstate model confidence. Showing a range
signals statistical honesty and is more defensible under judge
questioning about precision.

CONSTRAINTS
- The original point-estimate value must still be computed exactly as
  before — you are only changing how it's DISPLAYED, wrapping it in a
  range for presentation. Do not change the underlying scenario math.
- Apply this to all 5 scenario presets (including the Time Machine
  mode if already built) for consistency — a range on 4 presets and a
  single point on the 5th will look like an oversight.
- Verify the assumptions panel below still references the original
  point estimate in its text (e.g. "assumes X% drop") so the stated
  assumptions don't contradict the displayed range.

Show me the exact spread percentage you'll use and confirm it applies
uniformly before implementing.