import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sentinel-47 — Energy Supply Chain Resilience Platform",
  description: "AI-driven geopolitical risk intelligence, scenario simulation, adaptive crude procurement routing, and executive policy memo orchestration for India's energy security.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased dark`}
    >
      <body className="min-h-screen bg-cyber-bg text-gray-100 flex flex-col font-sans">
        {children}
      </body>
    </html>
  );
}
